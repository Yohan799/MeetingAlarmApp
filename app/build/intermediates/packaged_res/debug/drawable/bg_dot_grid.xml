<?xml version="1.0" encoding="utf-8"?>
<bitmap xmlns:android="http://schemas.android.com/apk/res/android"
    android:src="@drawable/ic_dot_pattern"
    android:tileMode="repeat" />
<!-- Note: referencing a vector in a bitmap tag might not work on older APIs. 
     Safe fallback: We will use a tiled View in layout or just use the gradient for now 
     if this causes issues. But for modern Android it *can* work if handled right, 
     or we just define the XML shape with repeated bitmap.
     Actually, let's try a different approach: A large vector with many dots? 
     No, that's inefficient.
     Let's try a shape with bitmap tile. To do that we need a PNG.
     Since I can't generate a PNG, I will use a programmatic approach 
     or just a simple "Grain" overlay if I can.
     I will skip the complex Dot Pattern for this iteration to avoid build errors 
     and stick to the Gradient + Orbs which are high impact.
-->
